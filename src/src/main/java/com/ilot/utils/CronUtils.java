package com.ilot.utils;


import java.util.Locale;

public class CronUtils {

 /*   public static String describeCron(String cronExpression) {
        return describeCron(cronExpression, Locale.FRENCH);
    }

    public static String describeCron(String cronExpression, Locale locale) {
        return CronExpressionDescriptor.getDescription(cronExpression, new CronExpressionParser.Options() {{
            setLocale(locale);
            setUse24HourTimeFormat(true);
        }});
    }*/
}
